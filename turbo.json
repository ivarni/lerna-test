{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["build/**"],
      "dependsOn": ["typecheck", "^build"]
    },
    "typecheck": {
      "outputs": []
    },
    "clean": {}
  }
}
